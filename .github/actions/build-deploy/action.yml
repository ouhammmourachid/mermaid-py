name: 'Build and Deploy'
description:  'Build and Deploy'
inputs:
  pypi-token:
    description: 'PyPI API token'
    required: true

runs:
    using: 'composite'
    steps:
      - name: Build package
        run: |
          make build
        shell: bash
      - name: Publish package
        run: |
          export 
        shell: bash
